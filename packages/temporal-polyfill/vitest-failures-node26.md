- [ ] Temporarily skip `funcApi/native` `ZonedDateTime.createFormat` in `src/funcApi/zonedDateTime.test.ts`; raw `Intl.DateTimeFormat#format` rejects native `Temporal.ZonedDateTime` in Node 26. The native adapter is forwarding to the host-native object as intended, so this remains covered by the forced-shim workspace until the formatter API is redesigned or the host behavior changes.
