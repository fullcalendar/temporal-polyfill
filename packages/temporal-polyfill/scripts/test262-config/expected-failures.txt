##
## In addition to the ignored tests below, see other files:
##
##   - expected-failures-node-*.txt
##     Shortcomings with native APIs, unrelated to our polyfill
##
##   - expected-failures-descriptor.txt
##     We construct our Temporal classes programmatically, not with ES6 `class`.
##     Thus, our property descriptors are not 100% compliant with the spec.
##
##   - expected-failures-builtin-calls.txt
##     Our polyfill calls methods like Array::push directly on objects,
##     which could be vulnerable to prototype pollution.
##     We are weighing cost/benefit of always calling static method references.
##


##
## Spec deviations (5 total)
##

# WONTFIX (1)
# The spec calls for higher floating-point division precision than what JS's
# 64-bit floats provide. proposal-temporal/polyfill achieves this via string
# manipulation hacks:
# https://github.com/tc39/proposal-temporal/blob/999937370cfd0f6d2ce3cc83a01ab82ab3771b46/polyfill/lib/timeduration.mjs#L92
#
# Our polyfill avoids the associated performance degradation and filesize
# increase by doing simple native JS number division, but this sometimes results
# in less precise results. Example:
#
#   OURS: 816.5692187493501
#   SPEC: 816.56921874935
#
built-ins/Temporal/Duration/prototype/total/precision-exact-mathematical-values-6.js

# TEMPORARY
# See DURATION-TOTAL-PRECISION-MEMORY.md. This representative Duration.total()
# test's months expectation matches separate JS Number arithmetic:
#
#   integerPart + numerator / denom
#
# We currently prefer the combined-division shape because it satisfies the more
# precision-specific NudgeToCalendarUnit test:
#
#   built-ins/Temporal/Duration/prototype/total/precision-exact-mathematical-values-5.js
#
# The proper fix is exact numerator construction plus one final rational-to-
# Number conversion, not another parenthesizing change.
built-ins/Temporal/Duration/prototype/total/relativeto-total-of-each-unit.js

# WONTFIX (2)
# Our polyfill does not fully support the lowest/highest year of some i18n
# calendars because:
#
# A) To convert between ISO dates and non-ISO YMD values, we must use
#    Intl.DateTimeFormat scraping to crawl through a year's months. The
#    `withCalendar` tests fail while reading non-ISO fields from an ISO edge
#    date; the `from` tests fail while resolving non-ISO fields back to an ISO
#    date. In both directions, if the requested date is within-bounds but the
#    calendar year boundary used for probing is not, our algorithm chokes.
#
# Failing code:
#   new Temporal.PlainDate(-271821, 4, 21, 'buddhist').month
#   // 2 days within epoch-milliseconds lower bound, but still fails
#
# B) -271821-04-19 is a valid PlainDate but it's out-of-range when computing UTC
#    epoch-milliseconds for Intl.DateTimeFormat. Possible to use a negative
#    offset time zone like America/Chicago, but this special-case is cumbersome.
#    Similar TimeClip issues happen at the upper edge when probing beyond
#    +8640000000000000 ms.
#
intl402/Temporal/PlainDate/from/extreme-dates.js
intl402/Temporal/PlainDate/prototype/withCalendar/extreme-dates.js
intl402/Temporal/PlainDateTime/from/extreme-dates.js
intl402/Temporal/PlainDateTime/prototype/withCalendar/extreme-dates.js
intl402/Temporal/PlainYearMonth/from/extreme-dates.js
intl402/Temporal/ZonedDateTime/from/extreme-dates.js
intl402/Temporal/ZonedDateTime/prototype/withCalendar/extreme-dates.js

# WONTFIX (3)
# We do not patch Intl.DateTimeFormat's constructor to more strictly validate
# timeZone strings
#
# A) Rejecting legacy ICU time zones: see expected-failures-node-gte16.txt
# B) Rejecting invalid minus sign:    see expected-failures-node-gte22.txt

##
## Issues with environment, already ignored in proposal-temporal/polyfill
## https://github.com/tc39/proposal-temporal/blob/main/polyfill/test/expected-failures.txt
##

# ENV-ISSUE
# Fails until Intl.DurationFormat available in Node.js release
intl402/Temporal/Duration/prototype/toLocaleString/returns-same-results-as-DurationFormat.js

# ENV-ISSUE
# Temporal test262 runner does not support $262.createRealm()
intl402/DateTimeFormat/proto-from-ctor-realm.js

# ENV-ISSUE
# https://github.com/tc39/ecma402/issues/402
intl402/DateTimeFormat/prototype/resolvedOptions/hourCycle-default.js


##
## TEMPORARY failures while fixing Stage 4 compliance
##

# WONTFIX for real - extension of WONTFIX (2)
# Temporal plain objects can represent dates at the edge of Temporal's ISO
# range, including instants that are outside JavaScript Date's TimeClip range.
# ECMA-402's Temporal bridge formats those plain values from Temporal format
# records / epoch nanoseconds and must not apply Date TimeClip.
#
# This polyfill's Intl.DateTimeFormat wrapper currently does a narrower bridge:
# it converts Temporal.PlainDate, Temporal.PlainDateTime, and
# Temporal.PlainYearMonth to epoch milliseconds and then delegates to native
# Intl.DateTimeFormat format-like methods with those numbers. Native Intl treats
# number inputs like Date-time values, so out-of-TimeClip values throw
# RangeError: Invalid time value before the formatter can produce string output
# or parts.
#
# Fixing these correctly requires a larger Temporal-aware formatting path for
# format, formatToParts, formatRange, and formatRangeToParts. A small patch that
# clamps, offsets, or string-rewrites native Intl output would be wrong because
# the tests cover calendar-sensitive fields, weekdays, ranges, and numbering
# systems. Keep these expected failures grouped until the bridge no longer
# relies on Date-bounded epoch milliseconds for Temporal plain objects.
intl402/DateTimeFormat/prototype/format/temporal-objects-no-time-clip-non-latin-numerals.js
intl402/DateTimeFormat/prototype/format/temporal-objects-no-time-clip-weekday.js
intl402/DateTimeFormat/prototype/format/temporal-objects-no-time-clip.js
intl402/DateTimeFormat/prototype/formatRange/temporal-objects-no-time-clip-weekday.js
intl402/DateTimeFormat/prototype/formatRange/temporal-objects-no-time-clip.js
intl402/DateTimeFormat/prototype/formatRangeToParts/temporal-objects-no-time-clip-weekday.js
intl402/DateTimeFormat/prototype/formatRangeToParts/temporal-objects-no-time-clip.js
intl402/DateTimeFormat/prototype/formatToParts/temporal-objects-no-time-clip-weekday.js
intl402/DateTimeFormat/prototype/formatToParts/temporal-objects-no-time-clip.js

# WONTFIX for real - TODO: combine with groups above
# Intl host-compliance gaps we intentionally do not paper over
#
# The polyfill wraps Intl.DateTimeFormat enough to preserve Temporal object
# input/output shapes and to make ordinary formatting behavior work, but it is
# not intended to become a string-level compatibility layer for every ECMA-402
# feature that the host Intl implementation has not caught up with yet.
#
# In particular, these failures come from Node/ICU behavior that would require
# patching formatted era strings or reimplementing Unicode-extension option
# resolution. That kind of broad Intl shim is brittle and out of scope unless a
# future fix is small, local, and clearly improves real Temporal behavior.
intl402/DateTimeFormat/prototype/formatToParts/era.js
intl402/DateTimeFormat/prototype/resolvedOptions/resolved-calendar-unicode-extensions-and-options.js
intl402/DateTimeFormat/prototype/resolvedOptions/resolved-numbering-system-unicode-extensions-and-options.js

# WONTFIX for real - TODO: combine with groups above
# Chinese/lunisolar DateTimeFormat output follows the host ICU data.
#
# The core Temporal calendar fields already carry a small amount of
# calendar-math correction where that is necessary for Temporal object
# behavior. Formatting is different: this package delegates formatted strings
# and formatToParts output to the host Intl.DateTimeFormat implementation. When
# Node's ICU4C data disagrees with the ICU4X data used by current test262 for a
# Chinese-calendar leap-month boundary, the formatted month part can imply the
# host's monthCode instead of the Temporal object's monthCode.
#
# One representative case is Chinese 1987:
#
#   Temporal.PlainDate.from("1987-08-23").withCalendar("chinese").monthCode
#   // "M06L" in this polyfill / test262 expectation
#
#   new Intl.DateTimeFormat("en", {
#     calendar: "chinese",
#     timeZone: "UTC",
#     year: "numeric",
#     month: "numeric",
#     day: "numeric",
#   }).formatToParts(Date.UTC(1987, 7, 23))
#   // host ICU4C reports month "7", which test262's bridge infers as "M07"
#
# Papering over this would mean rewriting localized DateTimeFormat parts for
# specific host ICU data quirks. That is brittle string-level Intl shimming, not
# Temporal polyfill behavior. Keep this documented as an expected failure so
# end users understand that Chinese/lunisolar formatting fidelity is bounded by
# their runtime's Intl/ICU data.
intl402/DateTimeFormat/prototype/formatToParts/compare-to-temporal-lunisolar.js

# WONTFIX for now
# Remaining Hebrew-calendar bridge/math failures.
#
# At this point, the broad era-model problems have been narrowed down to a
# single Hebrew-only cluster:
#
# - round-tripping a few RFC 9557 strings lands on a day that is off by one
#   for specific Hebrew dates that the tests mark as ICU4C/ICU4X discrepancies
# The important thing about this group is that it no longer looks like an era
# aliasing bug, parser bug, or a small validation-order issue. The remaining
# behavior sits in the Hebrew calendar math / bridge layer itself: exact date
# round-tripping for certain host-sensitive cases.
#
# A correct fix likely needs one of the following:
#
# - a more complete local Hebrew calendar field-math implementation, or
# - a carefully bounded Hebrew-specific bridge adjustment with strong evidence
#   that it is correct across the affected year shapes
#
# We do not want to paper over this by sprinkling ad hoc day bumps or special
# cases onto individual failing tests. That would be exactly the kind of broad,
# brittle Intl-shim behavior we are trying to avoid in this package. Until there
# is a small and clearly-correct Hebrew fix, keep this remaining cluster grouped
# here so the rest of the bucket can continue landing.
intl402/Temporal/PlainDate/from/roundtrip-from-string.js
intl402/Temporal/PlainDateTime/from/roundtrip-from-string.js
intl402/Temporal/ZonedDateTime/from/roundtrip-from-string.js
